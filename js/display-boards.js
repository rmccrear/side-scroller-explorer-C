
function showBoards() {
  // Display score
  fill(255, 255, 255); // White text
  textSize(24);
  textAlign(LEFT, TOP);
  text("Score: " + score, 10, 10);

// SCOREBOARD
    fill(255, 255, 255);
    textSize(24);
    textAlign(RIGHT, TOP);
    text("Health: " + health, 350, 10);
    

    // GAME OVER
    // if health runs out
    // show Game over
}